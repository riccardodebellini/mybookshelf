import 'package:flutter/material.dart';

class SettingsCreationPage extends StatelessWidget {
  const SettingsCreationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Creazione"),
        centerTitle: true,
      ),
      body: ListView(
        children: [
          ListTile()
        ],
      ),
    );
  }
}
